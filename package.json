{
  "name": "ai-incident-support-copilot",
  "version": "1.0.0",
  "description": "AI-powered incident support and ticket management system",
  "scripts": {
    "start": "./start.sh",
    "start:windows": "start.bat",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && source venv/bin/activate && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "test": "cd frontend && npm test",
    "build": "cd frontend && npm run build"
  },
  "keywords": [
    "ai",
    "support",
    "tickets",
    "incident-management",
    "copilot"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
